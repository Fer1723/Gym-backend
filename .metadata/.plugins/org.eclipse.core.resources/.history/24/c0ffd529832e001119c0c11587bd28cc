package gym_system.controller.com.mx;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import gym_system.entity.com.mx.Socio;
import gym_system.service.com.mx.SocioService;

@RestController
@RequestMapping("/api/socios")
@CrossOrigin(origins = "*")
public class SocioController {
	
	@Autowired
	private SocioService socioService;
	
	@GetMapping
	public List<Socio> listarSocios(){
		return socioService.obtenerTodos();
	}
	
	@PostMapping
	private Socio registrarSocio(@RequestMapping Socio socio) {
		return socioService.guardarSocio(socio);
	}

}
